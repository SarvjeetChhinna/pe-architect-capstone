# k8s/teams-api-canary-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: teams-api-canary-service
  namespace: engineering-platform
  labels:
    app: teams-api
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 4200
    targetPort: 8000
    protocol: TCP
    name: http
